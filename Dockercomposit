docker run -itd --name 9magentodb -e MYSQL_ROOT_PASSWORD=password -e MYSQL_USER=mike -e MYSQL_PASSWORD=password -e MYSQL_DATABASE=magento mysql bash
docker run -itd -p 80:80 --link 9magentodb:db magento2:3.1 bash


#remove all docker containers 
docker stop $(docker ps -a -q)
docker rm $(docker ps -a -q)


docker rmi $(docker images -q)
